[build]
builder = "nixpacks"
buildCommand = "pip install -e ."

[deploy]
startCommand = "mcp-google-search-console-crunchtools --transport streamable-http --host 0.0.0.0 --port 8017"
restartPolicyType = "on_failure"
